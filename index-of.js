const indexOf = (arr,value, optIndex) => {
    if (optIndex==undefined) {
        for (let i = 0; i < arr.length; i++) {
            if(value==arr[i]){
                return 
            }
        }
    }
};
